#ifndef DEFINE_base_h
#define DEFINE_base_h

#include <vector>
#include <string>

std::vector <std::vector <std::string> > initialize_map(const int&, const int&);
void print_map(const std::vector <std::vector <std::string> >&);

#endif
